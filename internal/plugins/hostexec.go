package plugins

// (No real adapter code lives here — the agentsvc.Hub adapter is wired in
// cmd/server/main.go where the Hub instance is in scope. This file is
// reserved for shared host-exec helpers and to anchor the test alongside.)
